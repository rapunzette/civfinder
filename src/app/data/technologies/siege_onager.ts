
import { Technology } from 'src/app/models/technology.model'
import { onager } from './onager'
export const siege_onager: Technology = {
  "name": "siege onager",
  "age": "imperial",
  "color": "blue",
  dependencies: [onager.name]
}
