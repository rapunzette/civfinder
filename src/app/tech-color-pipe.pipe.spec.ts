import { TechColorPipePipe } from './tech-color-pipe.pipe';

describe('TechColorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TechColorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
